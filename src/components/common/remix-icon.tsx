import { createIconSet   } from 'react-native-vector-icons';
import glyphMap from "@/assets/fonts/remixicon.glyph.json"

const CustomIcon = createIconSet(glyphMap,'remixicon','@/assets/fonts/remixicon.ttf')
export default CustomIcon;
